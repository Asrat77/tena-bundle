<%= form_with(model: lab_technician) do |form| %>
  <% if lab_technician.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(lab_technician.errors.count, "error") %> prohibited this lab technician from being saved:</h2>

      <ul>
        <% lab_technician.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
